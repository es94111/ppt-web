"use client";
import { FormEvent,useState } from "react";import { useRouter } from "next/navigation";
export function SettingsForm({name,email,role}:{name:string;email:string;role:string}){const router=useRouter();const[message,setMessage]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setMessage("");const value=String(new FormData(e.currentTarget).get("name"));const r=await fetch("/api/settings",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:value})});setMessage(r.ok?"已儲存":"儲存失敗");if(r.ok)router.refresh()}return <form className="card" style={{maxWidth:560}} onSubmit={submit}><div className="field"><label>顯示名稱</label><input className="input" name="name" defaultValue={name} required maxLength={80}/></div><div className="field"><label>Email</label><input className="input" value={email} disabled/></div><div className="field"><label>角色</label><input className="input" value={role} disabled/></div>{message&&<p className={message==="已儲存"?"success":"error"}>{message}</p>}<button className="btn">儲存設定</button></form>}
