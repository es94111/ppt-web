# PPT 線上瀏覽平台 — 開發文件（詳細版）

> 版本：v1.0
> 撰寫日期：2026-06-20
> 狀態：規格定稿，待開發

---

## 目錄

1. [專案概述](#1-專案概述)
2. [角色與權限模型](#2-角色與權限模型)
3. [技術棧選型與理由](#3-技術棧選型與理由)
4. [系統架構](#4-系統架構)
5. [資料庫設計](#5-資料庫設計)
6. [功能規格](#6-功能規格)
7. [API 設計](#7-api-設計)
8. [前端頁面與路由](#8-前端頁面與路由)
9. [安全性設計](#9-安全性設計)
10. [UI/UX 與美觀設計](#10-uiux-與美觀設計)
11. [瀏覽追蹤與稽核](#11-瀏覽追蹤與稽核)
12. [開發階段規劃](#12-開發階段規劃)
13. [部署與維運](#13-部署與維運)
14. [S3 物件儲存設計](#14-s3-物件儲存設計)
15. [測試策略](#15-測試策略)
16. [風險與待決事項](#16-風險與待決事項)

---

## 1. 專案概述

### 1.1 目標
建立一個 PPT（投影片）線上瀏覽與編輯平台。使用者透過網頁觀看由管理員/使用者建立的投影片簡報，支援角色權限、密碼保護、以及完整的瀏覽追蹤與 IP 記錄。

### 1.2 核心需求摘要
| 編號 | 需求 | 說明 |
|------|------|------|
| R1 | 線上瀏覽投影片 | 使用者可在線上逐頁觀看簡報，支援簡單的上一頁/下一頁切換 |
| R2 | 三種角色 | 管理員（Admin）、使用者（User）、訪客（Guest） |
| R3 | 管理員身分 | 系統初始化時，建立者即為 Admin（超級管理員） |
| R4 | 角色指派 | 管理員可在後台設定某帳號為 User 或 Guest |
| R5 | 投影片建立/編輯 | Admin 與 User 可在網頁上原生編輯投影片；Guest 不可編輯 |
| R6 | 簡報密碼保護 | 每份簡報可設定獨立的瀏覽密碼 |
| R7 | 瀏覽追蹤 | 記錄「誰、在何時、瀏覽了哪份簡報、第幾頁」 |
| R8 | IP 記錄 | 記錄所有瀏覽行為的來源 IP 位址 |
| R9 | 安全性 | 認證、授權、輸入驗證、XSS/CSRF/注入防護等 |
| R10 | 美觀 | 現代化、專業、響應式的 UI |

### 1.3 設計決策（已確認）
- **PPT 格式**：採用**網頁原生投影片**。投影片以結構化資料（JSON）儲存於資料庫，而非上傳 `.pptx` 檔案轉圖。如此 Admin/User 可直接在網頁編輯器中建立與修改，瀏覽時即時渲染。
- **技術棧**：Next.js 全端（理由見 §3）。
- **身分驗證**：同時支援 **Email + 密碼** 與 **Google OAuth** 兩種登入方式。

### 1.4 名詞定義
- **Deck（簡報集）**：一份完整的簡報，由多張 Slide 組成。對應「一份 PPT」。
- **Slide（投影片）**：簡報集中的單一頁面。
- **Element（元素）**：投影片上的內容物件（標題、文字框、圖片、形狀等）。
- **View（瀏覽記錄）**：一次瀏覽行為的稽核記錄。

---

## 2. 角色與權限模型

### 2.1 角色定義

| 角色 | 說明 | 取得方式 |
|------|------|----------|
| **Admin（管理員）** | 系統最高權限。可管理所有使用者、所有簡報、查看所有稽核記錄。 | 系統首位註冊者自動成為 Admin；之後可由現有 Admin 指派。 |
| **User（使用者）** | 可建立、編輯、刪除「自己的」簡報；可瀏覽有權限的簡報。 | 由 Admin 指派。 |
| **Guest（訪客）** | 唯讀。僅能瀏覽被授權或公開的簡報，不可建立/編輯/刪除。 | 預設新註冊者為 Guest（待 Admin 指派升級）。 |

> **預設角色策略**：新註冊帳號預設為 `GUEST`，需由 Admin 在後台手動升級為 `USER` 或 `ADMIN`。此為「最小權限原則」(Principle of Least Privilege)，避免任何人註冊即可上傳內容。

### 2.2 權限矩陣（RBAC）

| 操作 | Admin | User | Guest |
|------|:-----:|:----:|:-----:|
| 註冊 / 登入 | ✅ | ✅ | ✅ |
| 瀏覽公開簡報 | ✅ | ✅ | ✅ |
| 瀏覽受保護簡報（需密碼/授權） | ✅ | ✅（需符合授權） | ✅（需符合授權） |
| 建立簡報 | ✅ | ✅ | ❌ |
| 編輯**自己的**簡報 | ✅ | ✅ | ❌ |
| 編輯**他人的**簡報 | ✅ | ❌ | ❌ |
| 刪除**自己的**簡報 | ✅ | ✅ | ❌ |
| 刪除**他人的**簡報 | ✅ | ❌ | ❌ |
| 設定簡報密碼 | ✅ | ✅（自己的） | ❌ |
| 管理使用者角色 | ✅ | ❌ | ❌ |
| 查看全站瀏覽稽核記錄 | ✅ | ✅（僅自己簡報的記錄） | ❌ |
| 查看 IP 記錄 | ✅ | ✅（僅自己簡報的記錄） | ❌ |

> **權限檢查雙層防護**：前端依角色隱藏 UI（體驗），**但所有權限必須在後端 API 層再次強制驗證**（安全）。前端隱藏不等於安全。

### 2.3 簡報層級的存取控制
每份 Deck 有獨立的可見性設定：
- `PRIVATE`：僅擁有者與 Admin 可見。
- `PASSWORD`：任何擁有連結 + 正確密碼者可瀏覽。
- `PUBLIC`：任何登入使用者（含 Guest）可瀏覽。
- `UNLISTED`（選配）：知道連結即可瀏覽，不在列表中顯示。

---

## 3. 技術棧選型與理由

### 3.1 推薦技術棧

| 層級 | 技術 | 理由 |
|------|------|------|
| 框架 | **Next.js 15（App Router）** | 前後端一體，React Server Components + API Routes，減少維護兩個專案的成本；SSR 利於 SEO 與首屏速度。 |
| 語言 | **TypeScript** | 型別安全，降低執行期錯誤，編輯器與 API 的資料結構共用型別。 |
| UI | **Tailwind CSS + shadcn/ui** | 快速建立一致、美觀、可客製的元件；shadcn 元件可控、無黑箱。 |
| 資料庫 | **PostgreSQL** | 關聯式資料庫，適合使用者/簡報/稽核這類強關聯結構；支援 JSONB 儲存投影片內容。 |
| ORM | **Prisma** | 型別安全的資料存取、自動 migration、開發體驗佳。 |
| 認證 | **Auth.js (NextAuth v5)** | 原生支援 Credentials（Email/密碼）+ Google OAuth，Session 管理成熟。 |
| 投影片編輯器 | **自製 React 編輯器**（canvas/absolute 定位） | 完全掌控資料結構與權限；避免重型第三方依賴。 |
| 檔案儲存（圖片） | **S3 相容物件儲存**（Zeabur MinIO / Cloudflare R2） | 投影片內的圖片不放資料庫，改放物件儲存並存 URL。 |
| 部署 | **Docker / docker-compose** | 容器化打包 Next.js + PostgreSQL + 物件儲存，環境一致、可移植至任何支援 Docker 的平台（自架/雲端 VM/Zeabur）。詳見 §13。 |

### 3.2 為什麼是 Next.js 全端而非前後端分離？
- 本專案規模屬中小型，前後端分離會帶來雙重部署、CORS、型別不同步等額外成本。
- Next.js 的 Server Actions / API Routes 足以承載本專案的 CRUD 與認證需求。
- 單一 repo、單一部署、型別共用，開發與維護成本最低，最符合「快速做出安全又美觀的成品」的目標。

### 3.3 為什麼投影片用 JSON 結構而非存 .pptx？
- 需求明確要求「在網頁上編輯」。`.pptx` 在瀏覽器內編輯極為複雜（需重現 PowerPoint 渲染引擎）。
- JSON 結構（每張投影片 = 元素陣列）讓編輯、渲染、版本控制、權限控管都單純可控。
- 缺點：無法直接匯入既有 `.pptx`。若未來需要，可另開「匯入」功能（以 LibreOffice 轉圖作為唯讀附件），但不在 v1 範圍。

---

## 4. 系統架構

### 4.1 架構圖（邏輯）

```
┌─────────────────────────────────────────────────────────┐
│                    瀏覽器 (Client)                        │
│  ┌────────────┐  ┌────────────┐  ┌──────────────────┐    │
│  │ 瀏覽器播放器 │  │ 投影片編輯器 │  │ 管理後台 (Admin)  │    │
│  └────────────┘  └────────────┘  └──────────────────┘    │
└──────────────────────────┬──────────────────────────────┘
                           │ HTTPS (TLS)
┌──────────────────────────▼──────────────────────────────┐
│                  Next.js 應用伺服器                       │
│  ┌──────────────────────────────────────────────────┐    │
│  │ Middleware：認證檢查 / 角色守衛 / 速率限制 / 記錄 IP │   │
│  └──────────────────────────────────────────────────┘    │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────┐   │
│  │ Auth.js     │  │ API Routes   │  │ Server Actions  │   │
│  │ (登入/Session)│  │ (REST 風格)  │  │ (CRUD)          │   │
│  └─────────────┘  └─────────────┘  └─────────────────┘   │
│  ┌──────────────────────────────────────────────────┐   │
│  │ 服務層：權限服務 / 簡報服務 / 稽核服務 / 密碼驗證    │   │
│  └──────────────────────────────────────────────────┘   │
└───────────┬───────────────────────────┬─────────────────┘
            │ Prisma                     │ S3 SDK
┌───────────▼──────────┐    ┌────────────▼──────────────┐
│   PostgreSQL          │    │  物件儲存 (圖片資源)        │
│  users / decks /      │    │  decks/{id}/img/...        │
│  slides / view_logs   │    └───────────────────────────┘
└───────────────────────┘
```

### 4.2 請求流程範例：瀏覽一張投影片
1. Guest 開啟 `/d/{deckId}`。
2. Middleware 確認 Session（未登入則導向登入或要求密碼，依 Deck 可見性）。
3. 若 Deck 為 `PASSWORD`，先驗證密碼（成功後發短期 token cookie）。
4. Server 載入 Deck 與第 N 張 Slide 的 JSON，渲染為唯讀模式。
5. 同時非同步寫入一筆 `ViewLog`（user、deck、slide、IP、UA、時間）。
6. 回傳頁面；前端的「下一頁」按鈕僅切換 slide index，每次切換再寫一筆瀏覽記錄（或彙整後批次寫入，見 §11）。

---

## 5. 資料庫設計

### 5.1 ER 概觀
```
User 1───* Deck 1───* Slide
User 1───* ViewLog *───1 Deck
Deck 1───* DeckAccess（密碼通過或授權記錄，選配）
User 1───* Account（OAuth 綁定，Auth.js）
User 1───* Session（Auth.js）
```

### 5.2 Prisma Schema（草案）

```prisma
// 角色列舉
enum Role {
  ADMIN
  USER
  GUEST
}

// 簡報可見性
enum Visibility {
  PRIVATE    // 僅擁有者與 Admin
  PASSWORD   // 需密碼
  PUBLIC     // 所有登入者
  UNLISTED   // 知道連結即可
}

model User {
  id            String     @id @default(cuid())
  email         String     @unique
  name          String?
  image         String?
  passwordHash  String?    // Email 登入用；OAuth-only 使用者為 null
  role          Role       @default(GUEST)
  isActive      Boolean    @default(true)
  createdAt     DateTime   @default(now())
  updatedAt     DateTime   @updatedAt

  decks         Deck[]
  viewLogs      ViewLog[]
  accounts      Account[]  // Auth.js OAuth
  sessions      Session[]  // Auth.js
}

model Deck {
  id            String     @id @default(cuid())
  title         String
  description   String?
  ownerId       String
  owner         User       @relation(fields: [ownerId], references: [id], onDelete: Cascade)
  visibility    Visibility @default(PRIVATE)
  passwordHash  String?    // 設定密碼時填入（bcrypt/argon2 雜湊）
  coverImage    String?
  createdAt     DateTime   @default(now())
  updatedAt     DateTime   @updatedAt

  slides        Slide[]
  viewLogs      ViewLog[]

  @@index([ownerId])
  @@index([visibility])
}

model Slide {
  id        String   @id @default(cuid())
  deckId    String
  deck      Deck     @relation(fields: [deckId], references: [id], onDelete: Cascade)
  order     Int      // 投影片順序
  content   Json     // 投影片元素結構（見 §6.4）
  notes     String?  // 講者備註（選配）
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([deckId, order])
  @@index([deckId])
}

model ViewLog {
  id         String   @id @default(cuid())
  deckId     String
  deck       Deck     @relation(fields: [deckId], references: [id], onDelete: Cascade)
  userId     String?  // 未登入瀏覽者為 null（若允許匿名）
  user       User?    @relation(fields: [userId], references: [id], onDelete: SetNull)
  slideOrder Int?     // 瀏覽到第幾頁
  ipAddress  String   // 來源 IP（IPv4/IPv6）
  userAgent  String?
  referer    String?
  viewedAt   DateTime @default(now())

  @@index([deckId])
  @@index([userId])
  @@index([viewedAt])
  @@index([ipAddress])
}

// Auth.js 標準表（Account / Session / VerificationToken）依官方 schema 建立
```

> **重要**：`passwordHash`（無論使用者密碼或簡報密碼）一律以 **argon2id** 或 **bcrypt(cost≥12)** 雜湊儲存，**絕不**明文。

### 5.3 索引與效能
- `ViewLog` 將是成長最快的表，對 `deckId`、`userId`、`viewedAt`、`ipAddress` 建索引以利稽核查詢。
- 長期可考慮對 `ViewLog` 做分區（依月份）或定期歸檔。

---

## 6. 功能規格

### 6.1 認證（Authentication）
- **Email + 密碼**
  - 註冊：Email 格式驗證、密碼強度規則（至少 10 碼，含大小寫與數字）、密碼以 argon2id 雜湊。
  - 登入：失敗次數限制 + 速率限制（防暴力破解）。
  - 選配：Email 驗證信、忘記密碼流程。
- **Google OAuth**
  - 透過 Auth.js Google Provider。
  - 首次以 Google 登入者自動建立 User（角色 `GUEST`）。
  - 同 Email 帳號的綁定策略需定義（建議：相同 email 視為同一人，需驗證）。
- **Session**：HttpOnly + Secure + SameSite=Lax Cookie；JWT 或 DB Session 皆可（建議 DB Session 便於強制登出）。

### 6.2 角色管理（Admin 後台）
- 使用者列表：搜尋、分頁、顯示角色與狀態。
- 變更角色：`GUEST ⇄ USER ⇄ ADMIN`。
- 停用/啟用帳號（`isActive`）。
- **保護機制**：系統至少保留一名 Admin（不可把最後一位 Admin 降級）；Admin 不可停用自己。

### 6.3 簡報管理（Deck CRUD）
- 建立簡報（標題、描述、可見性）。
- 列表：Admin 看全部；User 看自己的 + 有權限的；Guest 看公開/授權的。
- 編輯簡報設定（標題、可見性、密碼）。
- 刪除簡報（軟刪除選配；硬刪除需二次確認）。

### 6.4 投影片編輯器（核心）
- **資料結構**：每張 Slide 的 `content` 為 JSON，例如：
```jsonc
{
  "background": "#ffffff",
  "elements": [
    { "id": "e1", "type": "text",  "x": 80, "y": 60, "w": 800, "h": 120,
      "text": "標題文字", "fontSize": 44, "fontWeight": 700, "color": "#111", "align": "left" },
    { "id": "e2", "type": "image", "x": 80, "y": 220, "w": 400, "h": 300,
      "src": "https://storage/.../img.png", "alt": "示意圖" },
    { "id": "e3", "type": "shape", "shape": "rect", "x": 540, "y": 220,
      "w": 360, "h": 300, "fill": "#4f46e5", "radius": 12 }
  ]
}
```
- **編輯能力（v1）**：新增/刪除投影片、調整順序、加入文字框/圖片/基本形狀、拖曳定位、改字體大小/顏色、設定背景。
- **權限**：僅 Admin 與簡報擁有者（User）可進入編輯模式；Guest 無編輯入口且後端拒絕其寫入。
- **儲存**：自動存檔（debounce）或手動存檔；每次寫入後端皆驗證權限與資料 schema（防止偽造 JSON 注入）。
- **重要安全點**：投影片文字渲染時**不可** `dangerouslySetInnerHTML` 注入未消毒內容；富文本需經白名單消毒（見 §9）。

### 6.5 瀏覽播放器（Viewer）
- 全螢幕/置中舞台，固定長寬比（如 16:9）。
- 控制項：上一頁、下一頁、頁碼指示（如 `3 / 12`）、鍵盤左右鍵、全螢幕。
- 唯讀渲染 Slide JSON。
- 每次切換頁面寫入瀏覽記錄。

### 6.6 簡報密碼保護
- 擁有者可為 Deck 設定/移除密碼（雜湊儲存）。
- 瀏覽 `PASSWORD` 類型 Deck 時出現密碼輸入頁；驗證成功後發放短期憑證（HttpOnly Cookie，綁定該 deckId，有效期如 2 小時）。
- 密碼驗證亦受速率限制保護。

---

## 7. API 設計

> 採 REST 風格（Next.js Route Handlers 或 Server Actions 實作）。所有寫入操作需通過認證 + 授權 + 輸入驗證（Zod）。

### 7.1 認證
| 方法 | 路徑 | 說明 | 權限 |
|------|------|------|------|
| POST | `/api/auth/register` | Email 註冊 | 公開 |
| —    | `/api/auth/[...nextauth]` | Auth.js（登入/登出/OAuth/Session） | 公開 |

### 7.2 使用者與角色（Admin）
| 方法 | 路徑 | 說明 | 權限 |
|------|------|------|------|
| GET | `/api/admin/users` | 使用者列表（分頁/搜尋） | Admin |
| PATCH | `/api/admin/users/:id/role` | 變更角色 | Admin |
| PATCH | `/api/admin/users/:id/status` | 啟用/停用 | Admin |

### 7.3 簡報與投影片
| 方法 | 路徑 | 說明 | 權限 |
|------|------|------|------|
| GET | `/api/decks` | 列出可見簡報 | 登入者 |
| POST | `/api/decks` | 建立簡報 | Admin/User |
| GET | `/api/decks/:id` | 取得簡報（含 slides） | 依可見性/密碼 |
| PATCH | `/api/decks/:id` | 更新設定（含可見性/密碼） | 擁有者/Admin |
| DELETE | `/api/decks/:id` | 刪除 | 擁有者/Admin |
| POST | `/api/decks/:id/verify-password` | 驗證簡報密碼 | 公開（速率限制） |
| POST | `/api/decks/:id/slides` | 新增投影片 | 擁有者/Admin |
| PATCH | `/api/decks/:id/slides/:slideId` | 編輯投影片內容 | 擁有者/Admin |
| PATCH | `/api/decks/:id/slides/reorder` | 調整順序 | 擁有者/Admin |
| DELETE | `/api/decks/:id/slides/:slideId` | 刪除投影片 | 擁有者/Admin |

### 7.4 瀏覽記錄與稽核
| 方法 | 路徑 | 說明 | 權限 |
|------|------|------|------|
| POST | `/api/decks/:id/view` | 記錄一次瀏覽（slideOrder + IP + UA） | 任何可瀏覽者 |
| GET | `/api/admin/view-logs` | 全站稽核查詢（篩選 user/deck/ip/時間） | Admin |
| GET | `/api/decks/:id/view-logs` | 單一簡報的瀏覽記錄 | 擁有者/Admin |

### 7.5 圖片上傳
| 方法 | 路徑 | 說明 | 權限 |
|------|------|------|------|
| POST | `/api/uploads/image` | 取得簽名上傳 URL / 直傳物件儲存 | Admin/User |

> 上傳需驗證檔案類型（白名單：png/jpeg/webp/gif）、大小上限、並重新編碼/驗證以防惡意檔案。完整的物件儲存設計（presigned URL 流程、bucket 結構、CORS/政策、生命週期清理）見 **§14**。

---

## 8. 前端頁面與路由

| 路徑 | 頁面 | 權限 |
|------|------|------|
| `/` | 首頁／登入入口／簡報牆 | 公開 |
| `/login` | 登入（Email + Google） | 公開 |
| `/register` | 註冊 | 公開 |
| `/dashboard` | 我的簡報（建立/管理） | Admin/User |
| `/decks/:id/edit` | 投影片編輯器 | 擁有者/Admin |
| `/d/:id` | 瀏覽播放器 | 依可見性 |
| `/d/:id/password` | 密碼輸入頁 | 公開 |
| `/admin/users` | 使用者與角色管理 | Admin |
| `/admin/logs` | 全站瀏覽稽核 | Admin |
| `/settings` | 個人設定 | 登入者 |

---

## 9. 安全性設計

> 安全性是本專案明確要求，以下為**必做**清單。

### 9.1 認證與 Session
- 密碼以 **argon2id**（或 bcrypt cost≥12）雜湊，加鹽，永不明文。
- Session Cookie：`HttpOnly`、`Secure`、`SameSite=Lax`。
- 登入/註冊/密碼驗證端點皆套用**速率限制**（如每 IP 每分鐘 N 次）與帳號鎖定，防暴力破解。
- 強制 HTTPS（HSTS）。

### 9.2 授權（Authorization）
- **後端為唯一信任邊界**：每個 API 都重新驗證「此 Session 的角色/擁有權是否允許此操作」。前端隱藏僅為體驗。
- 物件層級授權：操作 Deck/Slide 前確認 `ownerId === session.userId || role === ADMIN`。
- 防 IDOR：所有以 `:id` 取資源的端點都檢查存取權，不可只靠「猜不到 ID」。

### 9.3 輸入驗證與輸出編碼
- 所有輸入以 **Zod** 驗證（型別、長度、範圍、白名單）。
- 投影片 `content` JSON 在寫入前以 schema 嚴格驗證，拒絕未知欄位。
- 防 **XSS**：React 預設轉義；若需富文本，使用 **DOMPurify** 白名單消毒；嚴禁未消毒的 `dangerouslySetInnerHTML`。
- 防 **SQL 注入**：一律透過 Prisma 參數化查詢，不拼接 SQL。

### 9.4 CSRF
- Auth.js 內建 CSRF 保護；自訂的狀態變更端點使用 SameSite Cookie + CSRF token 或僅接受同源請求。

### 9.5 內容安全政策（CSP）與標頭
- 設定 CSP（限制 script 來源）、`X-Content-Type-Options: nosniff`、`X-Frame-Options: DENY`（防點擊劫持，瀏覽器頁可視需求調整）、`Referrer-Policy`。

### 9.6 檔案上傳安全
- 類型白名單 + 副檔名/MIME 雙重檢查 + 大小上限。
- 圖片重新編碼（去除惡意 payload / EXIF）。
- 上傳檔不可執行；以隨機檔名儲存於物件儲存，不放於可執行路徑。

### 9.7 密碼保護的簡報
- 簡報密碼雜湊儲存；驗證採固定時間比較。
- 通過後發短期 scoped cookie，不在 URL 帶密碼。

### 9.8 隱私與 IP 記錄合規
- 記錄 IP/UA 屬個資範疇：應於隱私政策揭露用途、保存期限（如 180 天）、與存取權限（僅 Admin）。
- 取得真實 IP 時，於反向代理後需正確解析 `X-Forwarded-For`（且僅信任自家代理鏈），避免 IP 偽造。
- 提供稽核記錄的保存期限與清理排程。

### 9.9 相依套件與機密
- 機密（DB 連線、OAuth secret、Session secret）放環境變數，不入版控。
- 啟用 Dependabot / `npm audit`；CI 加入 secret 掃描（gitleaks）與 SAST。

---

## 10. UI/UX 與美觀設計

### 10.1 設計原則
- 現代、專業、留白充足；以內容（投影片）為主角。
- 一致的設計系統（shadcn/ui + Tailwind tokens）。
- 完整響應式（桌面為主，平板/手機可瀏覽）。
- 深色/淺色模式。
- 無障礙：鍵盤可操作、focus 狀態、ARIA、對比度達 WCAG AA。

### 10.2 視覺風格建議
- 色彩：中性灰底 + 一個品牌主色（如 indigo/violet）作為強調。
- 字體：介面用無襯線（Inter / Noto Sans TC）；投影片內可選字體。
- 元件：卡片化簡報牆、清晰的角色徽章（Admin/User/Guest 不同顏色 badge）、平滑的頁面切換動畫（投影片左右滑動）。

### 10.3 關鍵畫面
- **簡報牆**：縮圖卡片網格，顯示標題、擁有者、可見性圖示（鎖/眼睛）、瀏覽數。
- **播放器**：極簡黑/白舞台，底部浮動控制列，左右大範圍點擊區切換頁。
- **編輯器**：左側投影片縮圖列、中央畫布、右側屬性面板（選取元素時顯示）。
- **Admin 後台**：資料表格 + 篩選 + 角色下拉。

---

## 11. 瀏覽追蹤與稽核

### 11.1 記錄內容（每筆 ViewLog）
- 誰：`userId`（未登入則記匿名 + IP）。
- 什麼：`deckId`、`slideOrder`。
- 何時：`viewedAt`。
- 從哪：`ipAddress`、`userAgent`、`referer`。

### 11.2 記錄時機
- 開啟 Deck 時記一筆「session 起始」。
- 每次切換投影片記一筆（或前端彙整每頁停留後批次送出，降低寫入量）。

### 11.3 IP 取得正確性
- 在 Zeabur/反向代理環境，從 `X-Forwarded-For` 取**最左側可信 IP**；於應用設定信任的代理數，避免 client 端偽造。

### 11.4 稽核查詢（Admin）
- 篩選維度：使用者、簡報、IP、時間範圍。
- 視圖：時間線、依簡報彙總、依使用者彙總、熱門頁分析。
- 匯出 CSV（選配）。

### 11.5 User 自助分析
- User 可查看「自己簡報」的瀏覽記錄與 IP，但看不到其他人的。

---

## 12. 開發階段規劃

> 建議分階段交付，每階段可獨立驗收。

### Phase 0：專案初始化（0.5 天）
- 建立 Next.js + TS + Tailwind + shadcn/ui。
- 設定 Prisma + PostgreSQL 連線。
- 設定 ESLint/Prettier、環境變數範本、CI（lint + build + secret scan）。

### Phase 1：認證與角色（2–3 天）
- Auth.js：Email/密碼 + Google OAuth。
- User 資料表、首位註冊者自動 Admin、預設 Guest。
- 路由守衛（middleware）。
- **驗收**：可註冊/登入/登出；Admin 可在後台改角色。

### Phase 2：簡報與投影片 CRUD（3–4 天）
- Deck CRUD + 可見性。
- Slide CRUD + 排序。
- 後端授權（擁有權/角色）。
- **驗收**：User 能建立簡報、新增投影片，Guest 不能。

### Phase 3：投影片編輯器（4–6 天）
- 畫布、文字/圖片/形狀元素、拖曳、屬性面板。
- 自動存檔 + schema 驗證。
- 圖片上傳至物件儲存。
- **驗收**：能在網頁完整編輯一份簡報並儲存。

### Phase 4：瀏覽播放器 + 密碼保護（2–3 天）
- 唯讀渲染、頁面切換、鍵盤/全螢幕。
- Deck 密碼設定與驗證流程。
- **驗收**：Guest 能逐頁瀏覽；受保護簡報需密碼。

### Phase 5：瀏覽追蹤與稽核（2–3 天）
- ViewLog 寫入（含正確 IP）。
- Admin 稽核頁 + 篩選；User 自助分析。
- **驗收**：能查到「誰在何時從哪個 IP 看了哪份簡報第幾頁」。

### Phase 6：安全強化與美化（2–3 天）
- CSP/標頭、速率限制、輸入消毒、依賴掃描。
- UI 打磨、響應式、深色模式、動效。
- **驗收**：通過安全檢查清單與 UI 走查。

### Phase 7：部署與驗收（1–2 天）
- 撰寫 Dockerfile + docker-compose（web + Postgres + MinIO）。
- entrypoint 跑 `prisma migrate deploy`；`.dockerignore`、非 root 執行。
- 反向代理 + HTTPS + 環境變數 + 網域。
- Trivy 掃描 image；上線煙霧測試。

> 總估約 **3–4 週**（單人、全職、含緩衝）。

---

## 13. 部署與維運

> **部署方式：Docker 容器化**。整個應用（Next.js + PostgreSQL + 物件儲存）以 Docker / docker-compose 打包，確保開發、測試、正式環境一致，可部署到任何支援 Docker 的平台（自架主機、Zeabur、雲端 VM 等）。

### 13.1 環境
- **開發**：`docker compose up`（Next.js dev + PostgreSQL + MinIO 一鍵起）。
- **正式**：Docker image 部署至目標平台；資料庫與物件儲存可用容器或託管服務。

### 13.2 容器化架構

```
┌──────────────────────────────────────────────────┐
│                 docker-compose                     │
│  ┌──────────┐  ┌────────────┐  ┌───────────────┐  │
│  │  web      │  │  postgres   │  │  minio        │  │
│  │ (Next.js) │──│ (DB)        │  │ (物件儲存)     │  │
│  │ :3000     │  │ :5432       │  │ :9000/:9001   │  │
│  └──────────┘  └────────────┘  └───────────────┘  │
│        持久化 volume：pgdata / minio-data           │
└──────────────────────────────────────────────────┘
```

### 13.3 Dockerfile（Next.js，多階段建置）

Next.js 需在 `next.config.js` 設定 `output: 'standalone'` 以產生精簡執行檔。

```dockerfile
# ---- 依賴階段 ----
FROM node:20-alpine AS deps
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci

# ---- 建置階段 ----
FROM node:20-alpine AS builder
WORKDIR /app
COPY --from=deps /app/node_modules ./node_modules
COPY . .
RUN npx prisma generate
RUN npm run build

# ---- 執行階段（精簡）----
FROM node:20-alpine AS runner
WORKDIR /app
ENV NODE_ENV=production
# 以非 root 使用者執行（安全）
RUN addgroup -g 1001 nodejs && adduser -u 1001 -G nodejs -S nextjs
COPY --from=builder /app/public ./public
COPY --from=builder --chown=nextjs:nodejs /app/.next/standalone ./
COPY --from=builder --chown=nextjs:nodejs /app/.next/static ./.next/static
COPY --from=builder /app/prisma ./prisma
USER nextjs
EXPOSE 3000
ENV PORT=3000
CMD ["node", "server.js"]
```

> 容器啟動時需先跑 migration：可用 entrypoint 腳本執行 `npx prisma migrate deploy` 再啟動 `server.js`，或於 compose 用獨立 init 服務跑 migration。

### 13.4 docker-compose.yml（開發/正式範例）

```yaml
services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: postgres://app:${POSTGRES_PASSWORD}@postgres:5432/pptweb
      AUTH_SECRET: ${AUTH_SECRET}
      AUTH_GOOGLE_ID: ${AUTH_GOOGLE_ID}
      AUTH_GOOGLE_SECRET: ${AUTH_GOOGLE_SECRET}
      S3_ENDPOINT: http://minio:9000
      S3_BUCKET: ${S3_BUCKET}
      S3_ACCESS_KEY: ${S3_ACCESS_KEY}
      S3_SECRET_KEY: ${S3_SECRET_KEY}
      TRUSTED_PROXY_COUNT: "1"
    depends_on:
      postgres:
        condition: service_healthy
      minio:
        condition: service_started
    restart: unless-stopped

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: app
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: pptweb
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U app -d pptweb"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  minio:
    image: minio/minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${S3_ACCESS_KEY}
      MINIO_ROOT_PASSWORD: ${S3_SECRET_KEY}
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio-data:/data
    restart: unless-stopped

volumes:
  pgdata:
  minio-data:
```

> 機密一律放在 `.env`（不入版控），由 compose 以 `${VAR}` 注入；正式環境改用平台的 secret 機制。

### 13.5 環境變數（範例）
```
# 應用
AUTH_SECRET=（隨機長字串，openssl rand -base64 32）
AUTH_GOOGLE_ID=...
AUTH_GOOGLE_SECRET=...
TRUSTED_PROXY_COUNT=1
# 資料庫
DATABASE_URL=postgres://app:PASSWORD@postgres:5432/pptweb
POSTGRES_PASSWORD=...
# 物件儲存
S3_ENDPOINT=http://minio:9000
S3_BUCKET=ppt-assets
S3_ACCESS_KEY=...
S3_SECRET_KEY=...
```

### 13.6 部署流程
1. 設定 `.env`（產生 `AUTH_SECRET`、填入 Google OAuth 與 DB/S3 密碼）。
2. `docker compose build`。
3. `docker compose up -d`。
4. 初次啟動跑 `prisma migrate deploy` 建表，並建立 MinIO bucket。
5. 反向代理（Nginx/Caddy/Traefik）在前面終止 TLS（HTTPS）並轉發到 `web:3000`，正確帶 `X-Forwarded-For`（呼應 §11.3 IP 記錄）。

### 13.7 Docker 安全要點
- 容器以**非 root**使用者執行（Dockerfile 已示範）。
- 使用固定版本的 base image（`node:20-alpine`、`postgres:16-alpine`），避免 `latest`。
- `.dockerignore` 排除 `node_modules`、`.env`、`.git`，避免機密/肥大 image。
- 定期重建 image 以套用 base image 安全更新；CI 加入 **Trivy** 掃描 image 漏洞。
- 資料庫與 MinIO 連接埠在正式環境不對外公開，僅內部網路可達。

### 13.9 GitHub Actions CI/CD（自動建置並推送 Docker image）

> 目標：push 到 `main`（或打 tag）時，GitHub Actions 自動建置 Docker image 並推送到 **Docker Hub**（或 GHCR）。

#### 流程
```
git push (main / tag v*)
        │
        ▼
┌─────────────────────────────────────────────┐
│ GitHub Actions: docker-publish.yml          │
│  1. checkout                                 │
│  2. 登入 Docker Hub（用 Secrets）            │
│  3. setup buildx（多平台/快取）              │
│  4. docker build                             │
│  5. docker push（tag: latest + commit SHA）  │
└─────────────────────────────────────────────┘
        │
        ▼
   Docker Hub: <user>/ppt-web:latest, :<sha>
```

#### 需設定的 GitHub Secrets
| Secret | 說明 |
|--------|------|
| `DOCKERHUB_USERNAME` | Docker Hub 帳號 |
| `DOCKERHUB_TOKEN` | Docker Hub Access Token（**不要用密碼**，於 Docker Hub → Account Settings → Security 產生） |

> 推 GHCR 則改用內建 `GITHUB_TOKEN`，無需額外 secret。

#### Workflow：`.github/workflows/docker-publish.yml`
```yaml
name: Build and Push Docker Image

on:
  push:
    branches: [main]
    tags: ["v*"]
  workflow_dispatch:   # 允許手動觸發

env:
  IMAGE_NAME: ppt-web

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write   # 推 GHCR 時需要
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ secrets.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=sha,format=short
            type=semver,pattern={{version}}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
```

#### 推 GHCR（GitHub Container Registry）的替代寫法
若想推到 `ghcr.io`（免額外 secret），把登入與 images 改成：
```yaml
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      # metadata-action 的 images 改為：
      #   images: ghcr.io/${{ github.repository }}
```

#### 建議的完整 CI Pipeline（與安全章節呼應）
建議在推送 image 前先跑品質與安全關卡，任一失敗即不推送：
1. **lint + type check + build**（`npm run lint`、`tsc --noEmit`、`npm run build`）
2. **測試**（Vitest 單元/整合）
3. **gitleaks** secret 掃描（呼應 §9.9）
4. **build Docker image**
5. **Trivy** 掃描 image 漏洞（呼應 §13.7）
6. **push**（僅 main/tag 通過上述步驟後）

> 可拆成多個 job 並用 `needs:` 串接：`test → security-scan → build-and-push`，PR 時只跑前段不推送，合併到 main 才推。

### 13.8 維運
- 資料庫定期備份（`pg_dump` 排程；volume 快照）。
- ViewLog 歸檔/清理排程（依保存政策）。
- 監控：錯誤追蹤（Sentry 選配）、容器健康檢查、基本可用性監控。
- 日誌：容器 stdout/stderr 集中收集。

---

## 14. S3 物件儲存設計

> 投影片內的圖片、簡報封面等**二進位資源不存入 PostgreSQL**，改放 S3 相容物件儲存，DB 僅存物件的 key/URL。本章詳述供應商選型、bucket 結構、上傳流程、安全、CORS/政策、程式碼與生命週期管理。與 §7.5（圖片上傳 API）、§9.6（檔案上傳安全）、§13（部署）相互呼應。

### 14.1 為什麼用物件儲存而非存資料庫？
- **效能**：大型二進位資料放 DB 會撐大資料列、拖慢備份與查詢；物件儲存專為大檔與高併發讀取設計。
- **成本與擴充**：物件儲存單價低、可水平擴充，且能直接掛 CDN 加速。
- **直傳能力**：可發 presigned URL 讓瀏覽器**直接上傳到 S3**，不經過 Next.js 伺服器，省頻寬與記憶體。
- **關注點分離**：應用伺服器無狀態，圖片資源獨立於應用容器，利於水平擴展。

### 14.2 供應商選型（S3 相容）

| 供應商 | 定位 | 優點 | 注意事項 |
|--------|------|------|----------|
| **MinIO（自架）** | 開發 + 自架正式 | 開源、可放進 docker-compose（見 §13.4）、完全自有資料 | 需自行維運、備份、擴容 |
| **Cloudflare R2** | 託管，正式推薦 | **免出口流量費（egress）**、S3 相容 API、可接 CDN | 需 Cloudflare 帳號；部分進階 S3 功能不支援 |
| **AWS S3** | 託管，企業級 | 功能最完整、生態成熟 | egress 計費、設定相對繁瑣 |
| **Zeabur 物件儲存 / 其他** | 託管 | 與部署平台整合 | 視平台支援度 |

> **建議策略**：開發與自架用 **MinIO**；正式環境若要省流量費，優先 **Cloudflare R2**。三者皆相容 S3 API，**程式碼一致，只需切換 endpoint 與金鑰**（見 §14.7），避免供應商鎖定。這也回應 §16.2 待決事項第 5 點。

### 14.3 Bucket 結構與物件 Key 命名

採單一 bucket（如 `ppt-assets`），以路徑前綴分類；key 內嵌 `deckId` 便於授權判斷與生命週期清理：

```
ppt-assets/
├── decks/
│   └── {deckId}/
│       ├── cover/{uuid}.webp          # 簡報封面
│       └── slides/{slideId}/{uuid}.webp   # 投影片內圖片
└── tmp/
    └── {userId}/{uuid}.{ext}          # 上傳暫存（未綁定，定期清理）
```

命名原則：
- **隨機檔名（UUID）**：避免可預測、避免覆蓋、避免路徑穿越（呼應 §9.6）。
- **不信任使用者原始檔名**：原始檔名僅供顯示，不用於 key。
- **副檔名以實際處理後格式為準**（統一轉 `.webp`，見 §14.6）。
- **以 `deckId` 為前綴**：刪除整份 Deck 時可用 prefix 批次刪除（見 §14.9）。

### 14.4 存取模型：私有 bucket + presigned URL（推薦）

預設 bucket **設為私有**（不開放匿名讀取），透過短期 presigned URL 控制上傳與下載：

**上傳流程（presigned PUT，瀏覽器直傳）：**
```
瀏覽器                    Next.js API                  S3 / R2 / MinIO
  │                          │                              │
  │ 1. POST /api/uploads/image (檔名,類型,大小,deckId)        │
  │─────────────────────────>│                              │
  │                          │ 2. 驗證權限 + 類型/大小白名單   │
  │                          │ 3. 產生 key + presigned PUT URL│
  │                          │<─────────────────────────────│
  │ 4. { uploadUrl, key }    │                              │
  │<─────────────────────────│                              │
  │ 5. PUT 檔案 (直傳，帶 Content-Type)                       │
  │──────────────────────────────────────────────────────> │
  │ 6. 200 OK                                               │
  │<────────────────────────────────────────────────────── │
  │ 7. PATCH slide content，寫入 key（不存完整 URL）          │
  │─────────────────────────>│                              │
```

**讀取（顯示圖片）兩種策略：**

| 策略 | 做法 | 適用 |
|------|------|------|
| **A. presigned GET** | 渲染前由後端為每個 key 產生短期 GET URL | 圖片需隨 Deck 權限控管（私有/密碼簡報），安全性高 |
| **B. 公開讀取 + CDN** | 將 `decks/*` 設為公開讀取，前面掛 CDN | 圖片本身不機密、追求最大讀取效能與快取 |

> **建議**：v1 採 **策略 A（presigned GET）**，與 §2.3 簡報可見性、密碼保護一致——私有/密碼簡報的圖片不會因為知道 URL 就外洩。若日後有大量公開簡報再評估策略 B + CDN。presigned GET 的有效期建議短（如 15–60 分鐘），渲染時即時簽發。

### 14.5 上傳安全要點（呼應 §9.6）
- **類型白名單**：僅允許 `image/png`、`image/jpeg`、`image/webp`、`image/gif`；同時檢查副檔名與 MIME。
- **以實際內容判斷類型**：用 magic bytes（檔頭）驗證，不只信前端宣告的 `Content-Type`。
- **大小上限**：在簽發 presigned URL 時用條件限制（如 POST policy 的 `content-length-range`），避免超大檔。
- **重新編碼**：上傳後以 `sharp` 重新編碼為 WebP，去除惡意 payload 與 EXIF（含 GPS 等隱私）。
- **隨機 key + 私有 bucket**：物件不可執行、不可列舉、不放於可執行路徑。
- **最小權限金鑰**：應用使用的 access key 僅授予該 bucket 的必要動作（見 §14.8），不要用 root 金鑰。
- **配額**：可對單一使用者/Deck 設總量上限，避免濫用塞爆儲存。

### 14.6 圖片處理（sharp）

上傳後（或在直傳後由後端非同步處理）統一正規化：

```ts
import sharp from "sharp";

// 將原圖轉為標準 WebP，限制最大尺寸並移除 metadata
export async function normalizeImage(input: Buffer) {
  return sharp(input)
    .rotate()                       // 依 EXIF 自動轉正後即丟棄 EXIF
    .resize({ width: 1920, height: 1920, fit: "inside", withoutEnlargement: true })
    .webp({ quality: 82 })
    .toBuffer();
}
```

> 若採「瀏覽器直傳」流程，重新編碼需改由後端**下載→處理→覆寫**，或改為「上傳到 `tmp/` → 後端處理後寫入正式 key」。簡化版可讓上傳先進 API、由後端處理後再 PUT 到 S3（犧牲直傳的頻寬優勢，換取一定能重新編碼）。**v1 建議走「經後端處理」流程以確保每張圖都被消毒。**

### 14.7 環境變數與 S3 Client（AWS SDK v3）

```bash
# 物件儲存（S3 相容）
S3_ENDPOINT=http://minio:9000        # MinIO；R2 為 https://<account>.r2.cloudflarestorage.com
S3_REGION=auto                       # R2 用 auto；AWS 用實際 region 如 ap-northeast-1
S3_BUCKET=ppt-assets
S3_ACCESS_KEY=...
S3_SECRET_KEY=...
S3_FORCE_PATH_STYLE=true             # MinIO 需 true；R2/AWS 視情況（R2 建議 true）
S3_PUBLIC_BASE_URL=                  # 若採公開讀取/CDN 才需要
```

```ts
// lib/s3.ts
import { S3Client } from "@aws-sdk/client-s3";

export const s3 = new S3Client({
  region: process.env.S3_REGION ?? "auto",
  endpoint: process.env.S3_ENDPOINT,
  forcePathStyle: process.env.S3_FORCE_PATH_STYLE === "true",
  credentials: {
    accessKeyId: process.env.S3_ACCESS_KEY!,
    secretAccessKey: process.env.S3_SECRET_KEY!,
  },
});

export const S3_BUCKET = process.env.S3_BUCKET!;
```

**產生 presigned 上傳 URL（API：`POST /api/uploads/image`）：**

```ts
// app/api/uploads/image/route.ts
import { PutObjectCommand } from "@aws-sdk/client-s3";
import { getSignedUrl } from "@aws-sdk/s3-request-presigner";
import { randomUUID } from "node:crypto";
import { z } from "zod";
import { s3, S3_BUCKET } from "@/lib/s3";
import { auth } from "@/auth";

const ALLOWED = { "image/png": "png", "image/jpeg": "jpg", "image/webp": "webp", "image/gif": "gif" } as const;
const MAX_BYTES = 10 * 1024 * 1024; // 10MB

const Body = z.object({
  deckId: z.string().cuid(),
  contentType: z.enum(Object.keys(ALLOWED) as [string, ...string[]]),
  size: z.number().int().positive().max(MAX_BYTES),
});

export async function POST(req: Request) {
  const session = await auth();
  if (!session || session.user.role === "GUEST") return new Response("Forbidden", { status: 403 });

  const parsed = Body.safeParse(await req.json());
  if (!parsed.success) return new Response("Bad Request", { status: 400 });
  const { deckId, contentType } = parsed.data;

  // 後端授權：確認此使用者可寫入該 deck（owner 或 admin）——呼應 §9.2
  await assertCanEditDeck(session, deckId);

  const ext = ALLOWED[contentType as keyof typeof ALLOWED];
  const key = `decks/${deckId}/slides/${randomUUID()}.${ext}`;

  const uploadUrl = await getSignedUrl(
    s3,
    new PutObjectCommand({ Bucket: S3_BUCKET, Key: key, ContentType: contentType }),
    { expiresIn: 300 }, // 5 分鐘
  );

  return Response.json({ uploadUrl, key });
}
```

**渲染時產生 presigned 讀取 URL（策略 A）：**

```ts
// lib/s3.ts（續）
import { GetObjectCommand } from "@aws-sdk/client-s3";

export function getViewUrl(key: string, expiresIn = 1800) {
  return getSignedUrl(s3, new GetObjectCommand({ Bucket: S3_BUCKET, Key: key }), { expiresIn });
}
```

> Slide `content` JSON 中的 `image.src` 建議**只存 key**（如 `decks/abc/slides/xxx.webp`），渲染前再由後端換成 presigned URL。如此可隨時切換供應商、調整存取策略，URL 過期也不影響資料正確性。對應需放寬 §附錄 B 中 `image.src` 的 `.url()` 驗證為「key 或 URL」。

### 14.8 Bucket 政策與 CORS

**最小權限 IAM 政策（應用金鑰僅能操作該 bucket）：**
```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Action": ["s3:PutObject", "s3:GetObject", "s3:DeleteObject"],
      "Resource": "arn:aws:s3:::ppt-assets/*"
    },
    { "Effect": "Allow", "Action": ["s3:ListBucket"], "Resource": "arn:aws:s3:::ppt-assets" }
  ]
}
```

**CORS（允許瀏覽器直傳 PUT；正式環境把 `AllowedOrigins` 限定為自家網域）：**
```json
[
  {
    "AllowedOrigins": ["https://你的網域"],
    "AllowedMethods": ["PUT", "GET"],
    "AllowedHeaders": ["Content-Type"],
    "ExposeHeaders": ["ETag"],
    "MaxAgeSeconds": 3000
  }
]
```

> bucket 預設 **Block Public Access / 不開匿名讀取**；只透過 presigned URL 存取（策略 A）。若採策略 B 才在 `decks/*` 開公開讀取。

### 14.9 生命週期與孤兒清理
- **刪除 Deck/Slide 時連帶刪物件**：刪 Slide → 刪該 slide 的圖片 key；刪 Deck → 用 `decks/{deckId}/` 前綴批次刪除（`ListObjectsV2` + `DeleteObjects`）。建議在 Deck/Slide 刪除的 service 層以交易+事後清理處理。
- **暫存清理**：對 `tmp/` 設 lifecycle rule，自動刪除 N 天前未綁定的上傳。
- **孤兒掃描**：排程比對「DB 中引用的 key」與「bucket 實際物件」，刪除無引用者（呼應 §13.8 維運）。
- **編輯時換圖**：更新元素圖片後，舊 key 若不再被任何 slide 引用即標記刪除。

```ts
// 刪除整份 Deck 的所有物件（前綴批次刪除）
import { ListObjectsV2Command, DeleteObjectsCommand } from "@aws-sdk/client-s3";

export async function deleteDeckAssets(deckId: string) {
  const prefix = `decks/${deckId}/`;
  let token: string | undefined;
  do {
    const list = await s3.send(new ListObjectsV2Command({ Bucket: S3_BUCKET, Prefix: prefix, ContinuationToken: token }));
    const objects = (list.Contents ?? []).map((o) => ({ Key: o.Key! }));
    if (objects.length) {
      await s3.send(new DeleteObjectsCommand({ Bucket: S3_BUCKET, Delete: { Objects: objects } }));
    }
    token = list.IsTruncated ? list.NextContinuationToken : undefined;
  } while (token);
}
```

### 14.10 本機/開發：MinIO 設定
- docker-compose 已含 `minio` 服務（見 §13.4）。
- 首次啟動需**建立 bucket** 並設 CORS。可用獨立 init 容器跑 `mc`：

```yaml
  minio-init:
    image: minio/mc
    depends_on:
      minio:
        condition: service_started
    entrypoint: >
      /bin/sh -c "
      mc alias set local http://minio:9000 $$S3_ACCESS_KEY $$S3_SECRET_KEY &&
      mc mb -p local/$$S3_BUCKET &&
      mc anonymous set none local/$$S3_BUCKET
      "
    environment:
      S3_ACCESS_KEY: ${S3_ACCESS_KEY}
      S3_SECRET_KEY: ${S3_SECRET_KEY}
      S3_BUCKET: ${S3_BUCKET}
```

> MinIO 主控台預設在 `:9001`，可用於檢視物件與設定政策。正式環境的 MinIO 連接埠不對外公開（呼應 §13.7）。

### 14.11 相依套件
```bash
npm i @aws-sdk/client-s3 @aws-sdk/s3-request-presigner sharp
```
> `sharp` 含原生二進位，於 Alpine 容器須確認可正常安裝（必要時於 Dockerfile 安裝對應系統函式庫，或改用 Debian 基底 image）。

### 14.12 測試重點（併入 §15 測試策略）
- Guest 呼叫 `/api/uploads/image` → 必須 403。
- User A 對 User B 的 deck 取簽名 URL → 必須 403（IDOR 防護）。
- 上傳非白名單類型或超過大小上限 → 拒絕。
- 偽造副檔名但內容非圖片 → magic bytes 檢查擋下。
- 刪除 Deck → 對應 `decks/{deckId}/` 物件全部清除，無孤兒殘留。
- presigned GET URL 過期後不可再存取。

---

## 15. 測試策略

| 層級 | 範圍 | 工具 |
|------|------|------|
| 單元測試 | 權限服務、密碼雜湊、Zod schema、IP 解析 | Vitest |
| 整合測試 | API 端點（授權矩陣、密碼驗證、CRUD） | Vitest + 測試 DB |
| E2E | 登入→建立簡報→瀏覽→稽核 主流程 | Playwright |
| 安全測試 | 授權繞過、IDOR、XSS、速率限制 | 手動 + 自動掃描 |

**重點測試案例**：
- Guest 嘗試呼叫編輯 API → 必須 403。
- User A 嘗試編輯 User B 的簡報 → 必須 403。
- 連續錯誤密碼 → 觸發速率限制。
- 注入含 `<script>` 的投影片文字 → 渲染時不執行。
- 偽造 `X-Forwarded-For` → 記錄到的仍是可信 IP。

---

## 16. 風險與待決事項

### 16.1 風險
| 風險 | 影響 | 緩解 |
|------|------|------|
| 自製編輯器工時超預期 | 進度延遲 | v1 先做最小元素集（文字/圖片/形狀），進階功能延後 |
| ViewLog 成長過快 | DB 膨脹/查詢慢 | 索引 + 歸檔 + 前端彙整批次寫入 |
| IP 偽造 | 稽核失真 | 正確設定可信代理鏈 |
| OAuth 帳號合併 | 重複帳號 | 定義 email 綁定策略 |

### 16.2 待決事項（需與你確認）
1. **匿名瀏覽**：未登入者是否可瀏覽 `PUBLIC` 簡報？（影響 ViewLog 的 userId 是否可為 null）
2. **投影片元素範圍**：v1 是否需要表格、圖示、簡單動畫，或僅文字/圖片/形狀？
3. **ViewLog 保存期限**：預設多久（建議 180 天）？
4. **是否需要簡報匯出**（PDF/圖片）功能？
5. **物件儲存供應商**：Zeabur MinIO 還是 Cloudflare R2？（選型比較見 §14.2；程式碼相容，僅需切換 endpoint/金鑰）
6. **網域**：是否已有正式網域要綁定？

---

## 附錄 A：權限檢查虛擬碼

```ts
async function assertCanEditDeck(session, deckId) {
  const deck = await db.deck.findUnique({ where: { id: deckId } });
  if (!deck) throw new NotFound();
  const isOwner = deck.ownerId === session.userId;
  const isAdmin = session.role === 'ADMIN';
  if (!isOwner && !isAdmin) throw new Forbidden(); // 後端強制
  return deck;
}
```

## 附錄 B：投影片 content JSON Schema（Zod 草案）

```ts
const ElementSchema = z.discriminatedUnion('type', [
  z.object({ type: z.literal('text'),  id: z.string(), x: z.number(), y: z.number(),
             w: z.number(), h: z.number(), text: z.string().max(5000),
             fontSize: z.number().min(8).max(200), color: z.string().regex(/^#[0-9a-fA-F]{3,8}$/),
             fontWeight: z.number().optional(), align: z.enum(['left','center','right']).optional() }),
  z.object({ type: z.literal('image'), id: z.string(), x: z.number(), y: z.number(),
             w: z.number(), h: z.number(), src: z.string().url(), alt: z.string().max(300).optional() }),
  z.object({ type: z.literal('shape'), id: z.string(), shape: z.enum(['rect','ellipse','line']),
             x: z.number(), y: z.number(), w: z.number(), h: z.number(),
             fill: z.string().regex(/^#[0-9a-fA-F]{3,8}$/), radius: z.number().optional() }),
]);

const SlideContentSchema = z.object({
  background: z.string().regex(/^#[0-9a-fA-F]{3,8}$/).optional(),
  elements: z.array(ElementSchema).max(200),
});
```

---

*文件結束 — v1.0*
