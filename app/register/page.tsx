import Link from "next/link";import { redirect } from "next/navigation";import { auth } from "@/auth";import { AuthForm } from "@/components/AuthForm";
export default async function Register(){if(await auth())redirect("/dashboard");return <main className="auth-wrap"><div className="auth-card"><h1>建立帳號</h1><p className="muted">第一位註冊者將成為系統管理員。</p><AuthForm mode="register" googleEnabled={Boolean(process.env.AUTH_GOOGLE_ID&&process.env.AUTH_GOOGLE_SECRET)}/><p className="muted" style={{textAlign:"center"}}>已有帳號？ <Link href="/login" style={{color:"var(--brand)"}}>登入</Link></p></div></main>}
